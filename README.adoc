= elx — Enterprise Linux Base Image
:toc: macro
:toclevels: 3
:toc-title:
:imagesdir: docs/images
ifdef::env-github[]
:tip-caption: :rocket:
:!showtitle:
:icons: font

++++
<p align="center">
    <h1 align="center">ELx</h1>
  
  
    <!-- REPLACE THE FOLLOWING WITH YOUR REPOSITORY/PROJECT'S SHORT DESCRIPTION -->
    <h3 align="center">a highly-customized Enterprise Linux Base Image for terminal-based workflows</h3>
        <p align="center">
      <a href="https://github.com/kriipke/elx-linux-image/actions/workflows/ci.yml"><img height=20 src="https://img.shields.io/github/actions/workflow/status/kriipke/elx-linux-image/ci.yml?branch=main&style=for-the-badge"></a>
  <a href="https://www.packer.io/"><img height=20 src="https://img.shields.io/badge/Packer-02A8EF?style=for-the-badge&logo=packer&logoColor=white"></a>
  <a href="https://rockylinux.org/"><img height=20 src="https://img.shields.io/badge/Rocky%20Linux-10B981?style=for-the-badge&logo=rockylinux&logoColor=white"></a>


</p>
<p align="center">
  <img src="docs/screenshot.png">
</p>
<br/>
++++

_A Packer-built, pre-hardened, batteries-included Enterprise Linux base image for building virtual machines._


`ELx` is a heavily-modified Enterprise Linux image, pressed with https://www.packer.io/[Packer], that
applies one identical set of bootstrapping tasks across releases of Enterprise Linux (Rocky Linux 9/10).
The result is a customized, minimal, ready-to-use VM image for CLI-based workflows — system administration
or a Linux daily-driver — that looks and behaves the same every time, as new EL releases come out.

In other words: instead of installing an OS by hand and then spending an afternoon configuring shells,
editors, login banners, and SSH hardening and installing your favorite command-line tools, you run a
single `packer build` and get back a reproducible VM image with all of that baked in — the same familiar
`ELx` terminal, shell, and CLI tool suite every time.

[.text-center]
*<<_whats_inside_the_image,What's inside>> · link:docs/building.adoc[Build it] · link:docs/contributing.adoc[Contribute] · link:docs/security.adoc[Security notes]*

toc::[]

'''

*At a glance*

[cols="1,3"]
|===
| Built with | https://www.packer.io/[Packer] + Kickstart, defined in `template/template.pkr.hcl`
| Targets    | Rocky Linux *10* (default) and *9*; aarch64 (Apple Silicon) out of the box, x86_64 configurable
| Builder    | Parallels (`parallels-iso`); swappable for QEMU / VirtualBox / VMware
| Ships with | zsh + Pure prompt, tmux auto-attach, vim + polyglot, and a curated CLI tool suite (`bat`, `fd`, `fzf`, `rg`, `nnn`, `lnav`, …)
| Hardened   | SSH locked down, authorized-use banners, self-describing `/etc/os-release`, predictable networking
| One command | `cd template && packer init . && packer build .`
|===


== What's inside the image

=== Base packages (installed during OS install via Kickstart)

Installed as RPMs by the Kickstart `%packages` section, e.g. `aide`, `bash-completion`, `vim-enhanced`,
`tmux`, `zsh`, `perl`, `bind-utils`, `openssh-clients`, `sudo`, `rsync`, `wget`, `net-tools`, `nfs-utils`,
and other common administration utilities.

=== Shell & editor experience

[cols="1,3"]
|===
| Component | Configuration applied

| *zsh*  | Set up with the https://github.com/sindresorhus/pure[Pure] prompt; system-wide config in `/etc/zshrc`.
| *tmux* | Auto-attaches a session on login and detaches/logs out cleanly (toggle with `TMUX_AUTO_ATTACH`). Requires tmux ≥ 3.2.
| *vim*  | Preloaded with the https://github.com/sheerun/vim-polyglot[vim-polyglot] language pack (system `vimfiles`).
|===

=== Command-line tools (installed as binaries via provisioner scripts)

[cols="1,2,1"]
|===
| Tool | Purpose | Pinned version

| https://github.com/sharkdp/bat[bat]      | `cat` with syntax highlighting        | 0.23.0
| https://github.com/schollz/croc[croc]    | Secure file transfer between machines | 9.1.1
| https://github.com/sharkdp/fd[fd]        | Fast, user-friendly `find`            | 8.7.0
| https://github.com/junegunn/fzf[fzf]     | Fuzzy finder (shell + vim + tmux)     | 0.39.0
| https://github.com/tstack/lnav[lnav]     | Log file navigator                    | 0.11.1
| https://github.com/jarun/nnn[nnn]        | Terminal file manager                 | 4.8
| https://github.com/BurntSushi/ripgrep[rg]| Fast recursive grep (via EPEL)        | latest EPEL
| https://github.com/rupa/z[z]             | Jump to frecent directories           | 1.11
| https://github.com/htop-dev/htop[htop]   | Interactive process viewer (via EPEL) | latest EPEL
|===

NOTE: Most versions are pinned inside each `template/scripts/install_*.sh` script — bump them there to
upgrade a tool. `rg` and `htop` are installed from EPEL and track whatever version EPEL ships for the
target Enterprise Linux release, so they work on both EL9 and EL10.

=== System hardening & housekeeping

* *SSH* — `UseDNS no` and `PasswordAuthentication no` set in `sshd_config`.
* *Login banners* — authorized-use warnings written to `/etc/issue` and `/etc/issue.net` (`issue.sh`),
  and a branded MOTD (`motd.sh`).
* *OS metadata* — `VARIANT`, `BUILD_ID`, and `DOCUMENTATION_URL` stamped into `/etc/os-release` so the
  image is self-describing (`metadata.sh`).
* *Networking* — predictable `eth0` naming and a `single-request-reopen` DNS latency fix.
* *SELinux* is left in `permissive` mode; the firewall is disabled (see the Kickstart file to change this).

== How it works

[source]
----
                    ┌──────────────────────────────────────────────┐
   packer build     │  1. parallels-iso builder boots the EL9 ISO  │
        │           │  2. Serves http/el9/ks.cfg over HTTP          │
        └──────────▶│  3. Kickstart does an unattended install     │
                    │  4. Reboots, Packer connects over SSH         │
                    │  5. shell provisioners run scripts/*.sh       │
                    │  6. cleanup.sh + minimize.sh shrink the image │
                    │  7. Image exported to builds/                 │
                    └──────────────────────────────────────────────┘
----

* The build is defined in `template/template.pkr.hcl`, with all of the tunable knobs (ISO, version,
  architecture, Kickstart file, credentials) declared in `template/variables.pkr.hcl`. It uses the
  *Parallels* builder (`parallels-iso`) and defaults to *Rocky Linux 10* on *aarch64 (Apple Silicon)*.
* Ready-made var-files select a target: `rocky10.pkrvars.hcl` (default) and `rocky9.pkrvars.hcl`.
* During install, Packer stands up a small HTTP server so the installer can fetch the Kickstart file
  named by the `kickstart` variable — `template/http/el10/ks.cfg` for Rocky 10, or `el9/ks.cfg` /
  `el8/ks.cfg` for older releases.
* The Kickstart file creates an unprivileged `ansible` build user with passwordless `sudo`. After first
  boot Packer logs in as that user over SSH and runs the provisioner scripts *in the exact order* listed
  in the `build` block, each via `sudo`.
* The last two scripts, `cleanup.sh` and `minimize.sh`, strip development packages and zero out free space
  so the exported disk compresses well.

=== Repository layout

[source]
----
.
├── template/
│   ├── template.pkr.hcl     # The Packer build definition (source + provisioner order)
│   ├── variables.pkr.hcl    # Input variables (ISO, version, arch, credentials, …)
│   ├── rocky10.pkrvars.hcl  # Var-file: build Rocky Linux 10 (aarch64) — the default
│   ├── rocky9.pkrvars.hcl   # Var-file: build Rocky Linux 9 (aarch64)
│   ├── http/
│   │   ├── el10/ks.cfg       # Kickstart answer file for Enterprise Linux 10
│   │   ├── el9/ks.cfg        # Kickstart answer file for Enterprise Linux 9
│   │   └── el8/ks.cfg        # Kickstart answer file for Enterprise Linux 8
│   └── scripts/             # Provisioner scripts, run in order by Packer
│       ├── motd.sh, issue.sh, metadata.sh   # branding & self-description
│       ├── sshd.sh, networking.sh           # hardening & network fixes
│       ├── configure_{vim,zsh,tmux}.sh      # shell/editor setup
│       ├── install_*.sh                     # CLI tool installers
│       ├── cleanup.sh, minimize.sh          # shrink the final image
│       └── _test.sh                         # helper to run scripts manually
├── bin/
│   └── gen_postinstall_script.sh   # Concatenate scripts into one post-install script
├── builds/                  # Build output (gitignored)
├── isos/                    # Place source ISOs here (gitignored)
├── Dockerfile               # Container for iterating on scripts without a full VM build
├── CHANGELOG
└── README.adoc
----


== Documentation

Step-by-step instructions live under link:docs/[`docs/`]:

[cols="1,3"]
|===
| Guide | What it covers

| link:docs/building.adoc[Building the images]  | Requirements, `packer` invocation, pointing at an ISO, and building for x86_64 or another hypervisor.
| link:docs/contributing.adoc[Contributing]     | Fast script iteration with Docker, generating a single post-install script, and customizing the image (tools, packages, branding).
| link:docs/security.adoc[Security notes]       | Default credentials and the hardening you should review before using the image anywhere untrusted.
|===

WARNING: This image is a *base for further automation*, not a production-ready hardened appliance. The
build ships with default credentials and authorized SSH keys — read link:docs/security.adoc[the security
notes] and replace them before building for anything untrusted.

== Changelog

See link:CHANGELOG[CHANGELOG] for the full history. Highlights:

* *Rocky Linux 10 support* — added `http/el10/ks.cfg` and `rocky10.pkrvars.hcl`; the build now defaults to
  the current Rocky 10 release. The template was parameterized via `variables.pkr.hcl` (ISO, version,
  arch, credentials), `htop` was added to the standard tool set, and `rg`/`htop` now install from EPEL so
  they work across EL9 and EL10.
* *v0.3-rc5* — Entire project rewritten in HCL (from the legacy JSON Packer template).
* *v0.3-rc3* — zsh/tmux integration.
* *v0.3-rc2* — tmux upgraded to 3.2 (new `tmux.conf` syntax); fixed zsh echoing CTRL sequences; added
  ShellCheck; added `z` to integrate with `nnn`.
